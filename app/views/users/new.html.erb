<% provide(:title, "Sign up") %>
<% provide(:button_text, "Create my account") %>
<% provide(:path, signup_path) %>
<h1>ユーザー登録</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <%= render "form" %>

    <div class="agreement">
      <%= link_to("登録することで、データに関するポリシー、Cookieポリシーに同意するものとします。",
                   agreement_path) %>
    </div>

  </div>
</div>
